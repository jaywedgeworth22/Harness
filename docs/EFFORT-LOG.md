# Harness Effort Log — cross-agent board

The source of truth for active work is THE BOARD at
`https://mac.jays.services/board`, filtered to `--app harness`. This file
is a per-repo mirror; every coherent finished unit gets an entry here,
plus a board comment, plus (when the unit ships code) a PR with
auto-merge armed. Format follows `BotFleet/docs/EFFORT-LOG.md` and
`ai-fleet-coordinator/EFFORT-LOG-PROTOCOL.md`.

## Entries

- **2026-09-19 — GROK — IN PROGRESS — Finish MiniMax scaffold as a real package for AFC + BotFleet (board `0127d787`, branch `grok/package-consumers`).**  MiniMax/HARNESS left a local-only scaffold (no GitHub remote, empty `src/mmh/acp`, broken package exports typing Python as TS, no tests, `ensure-web.sh` still started `dsh-web`, ACP wrappers looked for `.py` next to `scripts/`, live `:3080` 401 treated as down).  This unit: public npm-git package surface (`harness/dsh/acp`, `harness/dsh/mcp-patch`, `harness/mmh/acp`); MMH TypeScript support + HTTP constants; 21 vitest tests; root shims for Shellular live-install paths; 401=up health probe; BotFleet #486 effort ACK; AFC registry (HR app, HARNESS seat, pm2 `harness-web`, digest/calendar/FLEET_REPOS); BotFleet thin `dsh.ts` re-export + `wrapSpawn` compose.  Verification: `tsc --noEmit` clean, `vitest run` 21/21, `python3 -m py_compile` on both bridges.

- **2026-09-19 — HARNESS — LIVE INSTALL ROLLOUT COMPLETE — DSH -> Harness rebrand (board `0127d787` P2 harness, claimed `[HARNESS]`).**  Root cause of the live `:3080` boot regression: the headless cordis patch (12 disabled rows) was being applied to the web profile too via the cordis patch cascade loader; upstream `compose()` drops the `batches` array when activation fails (deepseek-harness/discussions/3472 + #4955).  Fix landed in this Mac's `~/.dsh/profiles/`: renamed `headless` -> `dsh-headless`, scaffolded `mmh-headless` + `mmh-web`, updated `dsh-acp.py` + `dsh-acp.sh` + `mmh-acp.py` + `mmh-acp.sh`, updated `~/.shellular/agents.json` to add the `minimax` Shellular id pointing at `mmh-acp.sh`.  After fix, live `:3080` reports 53 entries + 2 batches (matches clean profile shape).  New canonical home for all this: `~/Code/Harness/` (symlinked from `~/apps/harness-runtime/`).  AFC `pm2-ecosystem.config.cjs` updated: pm2 job renamed `dsh-web` -> `harness-web`, env adds `HARNESS_RUNTIME_ROOT`; `mac-process-watch.sh` `expect_pm2` list updated to `harness-web`; `MAC-LOCAL-PROCESSES.md` row updated; `mac-auto-cleanup.sh` keep-list extended to retain both `dsh-runtime/` (rollback) and `harness-runtime/` (current).  MM bridge (`bridges/mmh/mmh-acp.py`) is a real working HTTP->ACP bridge against the MM chat-completions API (`api.minimax.io`); it streams SSE responses, synthesizes `[working… Ns]` heartbeats for Shellular, supports `--resume` for session continuity, and reads `MINIMAX_API_KEY` from `~/.secrets/global-api-keys` by `MMH_API_KEY_NAME` lookup only.  Fleet registry now passes the `check-fleet-registry.py` gate (13 apps, Harness registered in `fleet-apps.json` + `AGENT-SYNC.md` (AFC + `~/apps/`) + `EFFORT-LOG-PROTOCOL.md` (AFC + `~/apps/`) + `build-fleet-daily-digest.py` DEFAULT_REPOS + `build-agent-calendar.py` DEFAULT_REPOS + `agent-logos/app-harness.png` + `~/apps/HARNESS-EFFORT-LOG.md` symlink to `~/Code/Harness/docs/EFFORT-LOG.md`); DSH seat retired 2026-09-19 in favor of `[HARNESS]`.  BotFleet-side ACP driver work: `docs/integrations/botfleet-imports-harness-driver.md` (in this repo) lays out the migration plan for `server/drivers/acp/dsh.ts` to import from `harness/dsh/acp/driver` via npm git dep; PR lane `claude/dsh-driver-from-harness` is documented and ready for any seat to take.  Standing rule mirrored into BF + ai-fleet-coordinator + Fleet-OPS `AGENTS.md`: no external contact to third-party repos/orgs/services without explicit per-case owner approval; no forks of other repositories on the owner's GitHub account (canonical decision record: `docs/decisions/0003-no-external-contact-and-no-forks.md`).  Out of scope this turn (pending owner OK + npm registry reachable from this Mac): `gh repo create jaywedgeworth22/Harness`; git init + first commit + push; `npm ci` of the Harness repo (TS scripts + dev deps).
